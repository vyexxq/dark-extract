/** Default WebSocket port for local development */
export const DEFAULT_WS_PORT = 2567;

/** Hub tick rate — server broadcasts player snapshots at this interval */
export const HUB_TICK_MS = 50;

/** Tile size in pixels (matches client Phaser grid) */
export const TILE_SIZE = 16;

/** Hub map dimensions in tiles */
export const HUB_WIDTH_TILES = 40;
export const HUB_HEIGHT_TILES = 30;

/** Player movement speed in pixels per second */
export const PLAYER_SPEED = 120;
